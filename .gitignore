# Local environment / secrets
.env
.env.local
*.lic
artifactory.lic
.licenses/

# Rendered configs (templates are committed as *.tmpl; outputs are local-only)
config/art1/system.yaml
config/art2/system.yaml
config/ldap/ldifs/*.ldif
!config/ldap/ldifs/*.ldif.tmpl

# Generated certificates and keys
config/nginx/certs/*
!config/nginx/certs/.gitkeep

# Docker / runtime data (volumes are named, but be safe)
data/
volumes/
.arti-deployer/

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
*~

# Logs
*.log
logs/

# Bootstrapped license files dropped into config dirs
config/art1/artifactory.lic
config/art2/artifactory.lic
