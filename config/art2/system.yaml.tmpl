# Minimal system.yaml for art2 — see config/art1/system.yaml.tmpl for the
# full explanation. Mission Control must be enabled on BOTH JPDs for the
# bind to succeed.
configVersion: 1

mc:
  enabled: true
