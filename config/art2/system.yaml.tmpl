## Artifactory 2 — system.yaml (TEMPLATE)
## Rendered by scripts/render-templates.sh from .env values.
configVersion: 1

shared:
  node:
    id: art2
    name: art2
  database:
    type: postgresql
    driver: org.postgresql.Driver
    url: jdbc:postgresql://postgres-art2:5432/artifactory
    username: artifactory
    password: ${PG_AF2_PASSWORD}
# Security keys omitted — see config/art1/system.yaml.tmpl note. The CoT
# bootstrap (scripts/configure-cot.sh) handles cross-instance trust.
