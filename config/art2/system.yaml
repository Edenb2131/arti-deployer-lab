## Artifactory 2 — system.yaml
configVersion: 1

shared:
  node:
    id: art2
    name: art2
    ip: artifactory2
  database:
    type: postgresql
    driver: org.postgresql.Driver
    url: jdbc:postgresql://postgres-art2:5432/artifactory
    username: artifactory
    password: art2_dev_password
  # ⚠ DEV-ONLY — same values as art1 so CoT bootstrap can succeed.
  security:
    joinKey: EEEE1111DDDD2222CCCC3333BBBB4444
    masterKey: FFFF0000EEEE1111DDDD2222CCCC3333
